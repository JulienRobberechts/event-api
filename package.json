{
  "name": "event-api",
  "version": "1.0.0",
  "description": "a small demo api to query events",
  "private": true,
  "scripts": {
    "cli": "yarn workspace event-cli start",
    "start": "yarn workspace server start",
    "test": "yarn workspace server test",
    "test-update": "yarn workspace server test-update"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "Julien Robberechts",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0",
    "yarn": "^1.22.4"
  },
  "resolutions": {
    "**/yargs-parser": "^18.1.3",
    "**/minimist ": "^1.2.3"
  }
}
